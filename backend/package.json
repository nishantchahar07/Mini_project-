{
  "name": "new-backend",
  "version": "0.1.0",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "init-db": "node scripts/init-db.js",
    "populate-texts": "node scripts/populate-texts.js",
    "populate-products": "node scripts/populate-products.js",
    "create-users": "node scripts/create-users.js",
    "setup": "npm run init-db && npm run populate-texts && npm run populate-products && npm run create-users"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
